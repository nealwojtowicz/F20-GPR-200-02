// GLSL STARTER CODE BY DANIEL S. BUCKSTEIN
//  -> IMAGE TAB (final)

/*
Name: Image
Purpose: This file simply calls the buffers and intiates the distorts
Contributors: Neal Wojtowicz
*/

//------------------------------------------------------------
// SHADERTOY MAIN

// mainImage: process the current pixel (exactly one call per pixel)
//    fragColor: output final color for current pixel
//    fragCoord: input location of current pixel in image (in pixels)


void mainImage( out vec4 fragColor, in vec2 fragCoord )
{
    sCoord uv = fragCoord / iChannelResolution[0].xy;
    
    fragColor = texture(iChannel0, uv); //calls Buffer A
    //fragColor = texture(iChannel1, uv); //calls Buffer B
    //fragColor = texture(iChannel2, uv); //calls Buffer C
}